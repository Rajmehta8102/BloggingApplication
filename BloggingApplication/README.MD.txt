A simple Blogging Application where user can add delete and update blogs along with comments.
We have used mongoDB cloud as the database.
For login and Sign in actvity we have used salt for hashing the password so to add an extra layer of security.
We can keep a track weather user is logged in or not using JWT cookie-parser on the deployed website.
Mongoose frame work is used to store and access data from MongoDB cloud database.
